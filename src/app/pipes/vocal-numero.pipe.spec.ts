import { VocalNumeroPipe } from './vocal-numero.pipe';

describe('VocalNumeroPipe', () => {
  it('create an instance', () => {
    const pipe = new VocalNumeroPipe();
    expect(pipe).toBeTruthy();
  });
});
